// The `filer` package implements the wikis link to the filesystem.
// It implements reading and writing pages, and provides a HTTP handler which
// simply delivers the raw file content.
package filer
