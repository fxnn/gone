// Package failer implements HTTP handlers that serve error pages.
// There is one common implementation of that HTTP handler, which is
// instantiated for each possibly served error page.
package failer
